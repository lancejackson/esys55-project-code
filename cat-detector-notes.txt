// backend logic
alarm = false
snooze = true

if digitalRead(irSensor) is ACTIVATED then
	set snooze to false
	set alarm to high

-- view logic -- 
if alarm equals high then
	<p>IR sensor activated</p>
else
	<p>IR sensor not activated</p>
// this might be confusing, real time updating in response to server side changes

-- routing logic --
if route equals '/L' then
	set snooze to true
